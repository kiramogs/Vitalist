{
    "name": "vitalist-api",
    "version": "5.0.0",
    "description": "Drug Adverse Effects Predictor API with ML + LLM",
    "engines": {
        "python": "3.11"
    },
    "scripts": {
        "start": "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
    }
}